import os from 'os';

// userInfo - return an object of User Information
console.log(os.userInfo());

// totalmem - return total memory
console.log(os.totalmem());

// freemem - total free memory
console.log(os.freemem()); 

// cpus() - return array object of CPU information  
console.log(cpus());